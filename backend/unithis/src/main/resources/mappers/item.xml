<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="item">

	<select id="getItemInfo" parameterType="java.lang.Integer" resultType="itemResponse">
		select * from item where id = #{id}
	</select>

	<select id="getItemsByCategory" parameterType="itemSearchRequest" resultType="itemResponse">
		select *
		from item
		where category=#{category} and address like 
		<foreach collection="address" item="add" separator=" or address like ">
		 	concat('%', #{add}, '%')
		</foreach>
		order by date desc limit #{idx}, 10
	</select>
	
	<select id="getAllItem" resultType="itemResponse">
		select * from item 
	</select>
	
	<insert id="createItem" parameterType="itemRequest" useGeneratedKeys="true" keyProperty="id" keyColumn="item.id">
		insert into(user_id, title, contents, category, need, address date)
		values(#{userId}, #{title}, #{contents}, #{category}, #{need}, #{address}, now())
	</insert>
	
	<update id="updateItem" parameterType="itemRequest">
		update item
		set title = #{title}, content = #{contents}, category = #{category}, need = #{need}, address = #{address}, date = now()
	</update>
	
	<update id="updateItemStatusWaiting" parameterType="java.lang.Integer">
		update item
		set status = "대기중"
		where id = #{id}
	</update>
	
	<update id="updateItemStatusSoldOut" parameterType="java.lang.Integer">
		update item
		set status = "거래완료"
		where id = #{id}
	</update>
	
	<update id="updateItemStatusOnSales" parameterType="java.lang.Integer">
		update item
		set status = "거래중"
		where id = #{id}
	</update>
	
	<delete id="deleteItem" parameterType="java.lang.Integer">
		delete from item
		where id = #{id}
	</delete>

</mapper>
